# from last year
# Artur gave up, too hard, scoring bot was not going through otherwise
SecRule REQUEST_URI "@beginsWith /portal_view_customizations" "id:1102,pass,t:none,nolog,ctl:ruleEngine=DetectionOnly"
